<%= render 'navbar' %>
<div class="container">
  <h1>Add list</h1>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.input :image_url %>
    <%= f.submit %>
  <% end %>
  <%= link_to 'Back', root_path %>
</div>
